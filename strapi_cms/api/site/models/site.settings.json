{
  "kind": "collectionType",
  "collectionName": "sites",
  "info": {
    "name": "site"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "role": {
      "type": "enumeration",
      "enum": [
        "production",
        "staging",
        "development"
      ],
      "required": true,
      "unique": true
    },
    "settings": {
      "type": "component",
      "repeatable": false,
      "component": "duples.site-settings-duple",
      "required": true
    },
    "nav_items": {
      "via": "site",
      "collection": "nav-item"
    },
    "pages": {
      "via": "site",
      "collection": "page"
    },
    "page_blocks": {
      "via": "site",
      "collection": "page-block"
    },
    "site_blocks": {
      "via": "site",
      "collection": "site-block"
    }
  }
}
